var evens = [0, 2, 4, 6, 8, 10];

evens.forEach(even => {
  console.log(`${even} is not odd!`);
}
);
console.log(evens.forEach(even));


var evens = [0, 2, 4, 6, 8, 10];

evens.forEach(even, index, array) => {
  debugger;
  console.log(`${even} is not odd!`);
});
// Add your doToElementsInArray() function here:


// Add your changeCompletely() function here:
